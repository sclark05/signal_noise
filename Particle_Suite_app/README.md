Particle Suite

A small multi-mode particle playground. Modes live in `modes/` and share a `core/particleEngine.js`.

How to run locally:

```bash
cd "/Users/scottclark/Local Sites/SignalNoise_apps"
python3 -m http.server 8000
# open http://localhost:8000/Particle_Suite_app/
```
